label: canonify URLs
name: canonifyURLs
widget: boolean
field_type: boolean
required: false
hint: This is a legacy configuration option, superseded by template functions and Markdown render hooks, and will likely be removed in a future release. See https://gohugo.io/content-management/urls/#canonical-urls
