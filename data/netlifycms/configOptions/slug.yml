label: Slug Type
name: slug
widget: object
hint: Changes how filenames for entries are created and sanitized. Also applies
  to filenames of media inserted via the default media library.
fields:
  - label: encoding
    name: encoding
    widget: select
    options:
      - unicode
      - ascii
    required: false
  - label: clean accents
    name: clean_accents
    widget: boolean
    required: false
  - label: sanitize replacement
    name: sanitize_replacement
    widget: string
    required: false
field_type: object
option_type: configOptions
required: false
collapsed: true
