label: default Content Language In Subdir
name: defaultContentLanguageInSubdir
widget: boolean
field_type: boolean
required: false
hint: Render the default content language in subdir, e.g. content/en/. The site
  root / will then redirect to /en/.
