label: hint
name: hint
widget: select
field_type: select
required: false
hint: |-
  Default hint about what type of image. Currently only used for WebP encoding.
  Default is "photo".
options:
  - picture
  - photo
  - drawing
  - icon
  - text
