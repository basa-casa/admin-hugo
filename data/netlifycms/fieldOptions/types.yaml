label: Types
name: types
widget: list
field_type: list
required: false
hint: "A nested list of object widgets. Every object widget may define different set of fields."
fields:
  - import: fieldOption fields.types
    field_type: import